<apex:component >
    <apex:attribute name="orgId" type="String" description="Organization ID" />
    <apex:attribute name="retURL" type="String" description="Return URL"  />
    
    <apex:pageBlock >
        <apex:form >
            <input type="hidden" name="oid" value="{!orgId}" />
            <input type="hidden" name="retURL" value="{!retURL}" />
        </apex:form>
    </apex:pageBlock>
</apex:component>